# Django
SECRET_KEY=
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Postgres — defaults in settings.py match a local dev install
DB_NAME=bikebuilder
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# AWS S3 (component + build images)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=

# Redis cache
REDIS_URL=

# Firebase admin credentials are read from a file, not env:
#   bikebuilder/firebase/firebase-credentials.json
